{
  "description": "With the PrimaryOnly default on a single virtual output, only four pinned DockItems for one dock surface are created.",
  "config": {
    "General/MonitorMode": 0
  },
  "actions": [],
  "assertions": [
    {
      "qa": "QA-VIS-018",
      "kind": "window",
      "key": "krema::DockView",
      "comment": "In PrimaryOnly mode, a dock window exists and is visible on the primary output",
      "frame": 10,
      "visible": true,
      "w": 1024,
      "h": 108
    },
    {
      "qa": "QA-VIS-018",
      "kind": "count",
      "type": "DockItem",
      "comment": "The single dock has only the four default pinned launchers, so the total DockItem count is exactly 4. If docks are duplicated, this count also increases by that multiple",
      "frame": 10,
      "value": 4
    }
  ]
}
